#include "structs.h"

bool queue_add(struct dataStruct* data, T element); // adds to back
elementType queue_peek(struct dataStruct data); // retrieves front
elementType queue_remove(struct dataStruct* data); // removes front 

//https://docs.oracle.com/javase/8/docs/api/java/util/Queue.html
